<?php
    // include('pdo.php');
    // $sobre = $_POST['sobre'];
    // $pdo->exec("DELETE FROM tb_sobre");
    // $sql = $pdo->prepare("INSERT INTO `tb_sobre` VALUES (null, ?)");
    // $sql->execute(array($sobre));

    // $sobre = $pdo->prepare("SELECT * FROM tb_sobre");
    // $validation = $sobre->execute();
    // $sobre = $sobre->fetch()['sobre'];

    // echo $validation;

?>